The register is only accurate by accident, and our benchmarks run 10% high

St Mary's sent a handoff marked "no action needed on either". Both halves needed
someone.

MARY-JOB-SESSION section 5c still told every chat the benchmarks were "averaging
7.9% out with almost no bias (-1.6% mean)". That was true of the first two
calibration entries and is not true of five. Verified all five at source rather
than take the numbers on trust - Greenfields +6.3, SM5 Wexham -9.5, Filwood +26.5,
Brocks Hill +18.7, St Mary's +10.2 - so four of five run high, mean bias +10.4%,
mean absolute error 14.2%. Confirmed exactly.

That section is what every chat reads to decide how much to trust a benchmark, so
a stale reassurance in it is worse than no guidance at all. It now carries the
table, the basis_type split, and the finding that matters more than the aggregate:
on St Mary's the whole-job error looked a respectable +4.4% only because the size
bands cancel. The register under-prices below 1.5m2 by a third and over-prices
above 3m2 by a third. A job weighted toward one band will come out badly wrong in
that direction, so the unit mix now has to be stated on the face of the document.

Also recorded the two mechanical traps: three of the four typed CALIBRATION
corrections are upward multipliers, which compound a base already running high;
and derived_factors() from learned-rates.json supersedes the hand-typed list, so
on a BSW job the measured factor fires and the Sheerline +10% never runs at all.
Nothing changed in the engine - five points cannot move a factor built on 273
lines.

Second, the estimating@ workaround is now an explicit prohibition rather than a
matter of judgement. My own probe yesterday evening is what makes it tempting: it
showed estimating@ still inside the app policy while mary@ is out, so routing
Mary's outbound through it would probably work. The reason it must not, which
neither of us had stated, is that the Exchange transport rule caging Mary's
recipients is scoped to mary@. Sending from estimating@ removes the server-side
guarantee entirely, and a mis-addressed message could reach a client or a supplier
- the one failure the ghost protocol exists to prevent. It would also make Mary
indistinguishable from Gintare in the team's own mailbox.

In AI.md with that reasoning and appended to REQ-23, so it is not implemented as a
helpful fix from either direction.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
