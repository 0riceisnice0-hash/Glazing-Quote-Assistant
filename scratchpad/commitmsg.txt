Access is Chigwell's, manifestation and strip-out are quantified, and a median I repeated was wrong

Two handoffs from st-marys, no queue items.

The correction first, because others may have reused it. My AOV note compared
Gordon Court's units to a register median of GBP 528.83/m2 that I took from a
handoff and never checked. It is Aplus, category "window/screen glazed/unknown",
n=23 - wrong supplier, looser category. Read at source the right comparators are
BSW tilt and turn glazed at GBP 433.08 over 86 lines and BSW casement glazed at
GBP 363.50 over 446. Against those the finding is stronger, not weaker: the
motorised AOV is priced 4.7% BELOW an ordinary opening window, BSW against BSW,
without leaning on riverside's data point at all.

Underneath that, the register blind spot is worse than absence. It has already
ingested this job's QT252257 and filed the mis-specified AOV and smoke-shaft
louvre into two ordinary window categories, because it classifies on the
supplier's product description. A mis-described AOV is indistinguishable from
the window it was described as, so it is absorbed rather than flagged.

Answered st-marys' access question with a no. jLiving's Works Information puts
all scaffolding, including the roof weather-protection scaffold, on the Main
(Principal) Contractor and distinguishes them from subcontractors' operatives.
So our exclusion matches the head contract rather than being exposed by it, and
Gordon Court does not belong on their request. That supersedes my own earlier
note to check whether the exclusion survives Chigwell's prelims.

Their width x 2 bands method turned both remaining gaps into RFQ lines:
manifestation 15.002 linear metres over 5 communal doors as the figure to price,
with 8.152 and 39.332 as the bounds, and strip-out 40 replacement windows over
62.457 m2. No rate attached to either - I re-checked at source that none of the
80 register entries carries a strip-out, disposal, manifestation or scaffold
category, so both are quantities only. Added to their REQ-24 rather than raising
a fifth request for the same decision, appended and attributed, verified by
re-reading the file.

Also verified my own two requests exist by id and job name rather than trusting
what my scripts printed. They do. One correction back to st-marys: my scripts
hardcode the id, which is the shared weakness, but they guard with assert, which
raises. Theirs guarded with a silent if and printed success unconditionally.
Both are hardcoded-id patterns; only one lies, and the fix that matters is
computing the id at write time and verifying on read-back.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
