THE REGISTRY FIX IS REAL BUT INERT UNTIL THE BRIDGE RESTARTS - REQ-18 RAISED. St Mary's was right: five briefs were sitting undeliverable (riverside, chester-thomas, ninn-lane, manor-house, lower-range). All five jobs are re-added and all seven pending handoffs now map to a live key - checked, zero orphans.

THE ACTUAL CAUSE, which my 17:05 note only half had. It is not just that save_registry() overwrote. `mary_bridge.py` line 429 loads the registry ONCE at startup and then writes that same in-memory snapshot back on every session start and end (lines 233 and 316). So the bridge does not merely fail to notice a new job - it actively restores the world as it was when it booted, every time any chat runs. That is why the same five jobs kept coming back deleted no matter how often I re-added them.

Both causes are now fixed in code: save_registry() re-reads and merges, and the bridge re-reads the registry at the top of each pass instead of holding a snapshot. BUT the running bridge is pythonw pid 31876, started 15:51:24 - before the fix landed. A long-running Python process keeps the module it imported at startup, so it is still executing the OLD code and the fix cannot bite. Expect the five jobs to be wiped again on the next session until Zac restarts it. I have not restarted it myself: it is the process that launches our sessions, so killing it mid-turn would end the session doing the killing.

WHAT THIS MEANS FOR YOU UNTIL THE RESTART: if you open a job chat or edit a match list, assume it may not survive. Run `python scripts\mary_router.py --list` at the end of your turn and check test-results\mary-inbox\handoffs\ for notes addressed to keys that no longer exist. If your own job key has vanished, re-add it - a chat that has never run loses nothing, but one that HAS run loses its whole conversation, and data\jobs\<key>.md is the only backup. That is the real argument for keeping your job file current rather than leaving it to close-out.

GENERAL LESSON WORTH KEEPING AFTER THIS IS OVER: when a fix does not appear to work, check whether the process you are fixing is long-running and loaded the old code. Editing a file changes what the NEXT process does, not what the running one is doing.
